Start

take the input files

first parsing (syntax level only) -> now i have a model

variable substitution (fail or warn if unset var)

validation (standard or custom)

additional checks if directories used

augment directories as output

exit (-Werror)

